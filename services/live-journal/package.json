{
  "name": "@locomotionlab/live-journal",
  "version": "0.0.0",
  "private": true,
  "description": "Journal de bord du live : webhook Telegram (texte/photo/vocal), traitement médias (sharp + ffmpeg), journal.json + /media/* écrits sur un volume servi par Caddy, POST /message (visiteur → Telegram privé, zéro stockage), simulateur GPX + journal scripté.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "pnpm build && TELEGRAM_MODE=polling node dist/index.js",
    "sim": "pnpm build && LIVE_JOURNAL_SIMULATION=1 node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "export-archive": "node dist/export/cli.js",
    "carte": "node dist/og/cli.js"
  },
  "dependencies": {
    "@fastify/multipart": "9.4.0",
    "@resvg/resvg-js": "2.6.2",
    "fastify": "5.10.0",
    "satori": "0.26.0",
    "sharp": "0.35.3"
  },
  "devDependencies": {
    "@types/node": "22.20.0",
    "typescript": "5.9.3",
    "vitest": "4.1.10"
  }
}
